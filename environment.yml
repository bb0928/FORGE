name: homierl
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - torch==2.4.1
      - torchvision==0.19.1
      - --index-url https://download.pytorch.org/whl/cu121
      - -r requirements.txt
